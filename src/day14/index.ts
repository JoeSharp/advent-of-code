import { AdventFunction } from "../common/types";

const day14: AdventFunction = (filename = "./src/day14/input.txt") => {
  return new Promise((resolve) => {
    resolve([1, 1]);
  });
};

export default day14;
