import { AdventFunction } from "../../common/types";

const dayX: AdventFunction = (filename = "./src/2024/dayX/input.txt") => {
  return new Promise((resolve) => {
    resolve([1, 1]);
  });
};

export default dayX;
