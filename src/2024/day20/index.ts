import { AdventFunction } from "../../common/types";

import { CROSS_DIRECTIONS, Position } from "../../common/arrayUtils";
import { RawMaze, loadRawMaze } from "../../common/mazeUtils";

// Assumes there is only one path
export function findPath(rawMaze: RawMaze): Position[] {
  let route: Position[] = [];

  return routes;
}

const day20: AdventFunction = async (
  filename = "./src/2024/day20/input.txt",
) => {
  return [1, 1];
};

export default day20;
